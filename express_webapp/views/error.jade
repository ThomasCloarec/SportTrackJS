doctype html
//
  ?php
  if (isset($_SESSION['error']['message']) && isset($_SESSION['error']['return'])) {
  $error = $_SESSION['error']['message'];
  $page = $_SESSION['error']['return'];
  }
  ?
html(lang='fr')
  head
    meta(charset='utf-8')
    meta(name='viewport' content='width=device-width, initial-scale=1, shrink-to-fit=no')
    title Inscription
    // CSS THEME
    link(rel='stylesheet' href='resources/css/vendor.bundle.base.css')
    link(rel='stylesheet' href='resources/css/style.css')
  body#body
    .container-scroller
      .container-fluid.page-body-wrapper.full-page-wrapper
        .content-wrapper.d-flex.align-items-center.theme-one
          .row.w-100.mx-auto
            .col-lg-8.col-xl-6.mx-auto
              .auto-form-wrapper
                .row.mt-5
                  .col-12.d-flex.text-center.justify-content-center
                    span(style='color: red;')
                      #{msg}
                      // ?php echo (empty($error)) ? '404 : Destination inconnue.' : $error ?
                .row
                  .col-12.mt-5.d-flex.justify-content-center
                    form(action='' method='POST')
                      input(type='hidden' name='page' value="#{ret}")
                      .col-12.mt-2.mb-2
                        button.btn.btn-primary(type='submit') Retour
    //
      ?php
      unset($_SESSION['error']);
      ?
